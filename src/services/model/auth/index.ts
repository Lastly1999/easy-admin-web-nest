export type ILoginForm = {
    userName: string
    passWord: string
    captchaId: string
    captchaCode: string
}

export type ILoginInfo = {
    accessToken: string
}

export type IGraphicCodeInfo = {
    cap: string
    captchaId: string
}

export type IRoleAuthMenus = {
    
}